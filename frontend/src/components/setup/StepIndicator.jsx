// Phase 1 stub — real content built in Phase 4.
import React from 'react';

export default function StepIndicator({ step, total }) {
  return (
    <div>
      <h2>StepIndicator (step {step} of {total})</h2>
    </div>
  );
}
