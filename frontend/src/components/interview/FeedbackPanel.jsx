// Phase 1 stub — real content built in Phase 7.
import React from 'react';

export default function FeedbackPanel({ evaluation, mode }) {
  return (
    <div>
      <h2>FeedbackPanel</h2>
    </div>
  );
}
