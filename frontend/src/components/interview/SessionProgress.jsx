// Phase 1 stub — real content built in Phase 7.
import React from 'react';

export default function SessionProgress({ current, total }) {
  return (
    <div>
      <h2>SessionProgress ({current}/{total})</h2>
    </div>
  );
}
