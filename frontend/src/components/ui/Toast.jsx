// Phase 1 stub — real content built in Phase 9.
import React from 'react';

export default function Toast({ message, variant, onDismiss }) {
  return (
    <div>
      <h2>Toast ({variant}): {message}</h2>
    </div>
  );
}
